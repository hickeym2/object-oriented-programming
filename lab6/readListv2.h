#include <iostream>
#include <cstdlib>
#include <string>
#include <fstream>
#include <sstream>
using namespace std;

int readListv2(entry_qb *list, entry_qb *head){
	if(list == NULL){
		cout << "No available list." << endl;
	}else{
		do{
			
		}while(temp != head);
	}
}//end of readListv2
